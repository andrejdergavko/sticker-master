export enum Pages {
  print = 'print',
  fileSelection = 'fileSelection',
}

export enum Routes {
  print = '/',
  fileSelection = '/file-selection',
}

export enum Providers {
  almik = 'almik',
  arclow = 'arclow',
}
